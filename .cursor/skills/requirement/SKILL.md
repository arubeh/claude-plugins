---
name: requirement
description: 対話的に要件を深掘りし、AIパイプライン（/issue-create → /issue-flow）が推測ゼロで動ける要件定義書を docs/requirements/ に生成する。Use when user wants to create a requirements document (e.g. /requirement 入居者向け家賃明細閲覧機能).
---

# Requirement スキル

対話的に要件を深掘りし、`/issue-create` → `/issue-flow` パイプライン全体が **推測ゼロ** で動ける要件定義書を生成します。

## 使い方

```
/requirement 入居者向け家賃明細閲覧機能
/requirement 社内ナレッジ検索システムをゼロから作りたい
/requirement docs/rough-idea.md この走り書きを要件に仕上げたい
```

**引数（何を作りたいかの説明）は必須。** 空の場合は「要件を指定してください」と案内して中断。

## 設計思想

この要件定義書は人間だけでなく、下流の全エージェントが消費する:

| 要件定義のセクション | 消費するエージェント | なぜ必要か |
|-------------------|-------------------|-----------|
| 目的・背景 | issue-create | Issue 本文の「背景」に転記 |
| アクター・ユースケース | planner | 画面遷移・API 設計の根拠 |
| 機能要件 | issue-create | Issue 分割の単位 |
| データモデル | planner, architect-reviewer | スキーマ設計・FK 整合性チェック |
| 非機能要件・制約 | tech-selector | 技術決定ポイントの絞り込み |
| スコープ外 | planner, issue-create | 余計な実装の防止・Phase 振り分け |
| 受け入れ条件 | TDD Phase 2 | RED テストの元ネタ |
| 用語集 | 全エージェント | 命名の一貫性 |

**書かないもの**: 技術スタック詳細（tech-selector が判定）、ファイル構成（planner が導出）、API 仕様詳細（planner が生成）。

## 実行フロー

```
引数チェック（空なら中断）
    │
Step 0: コンテキスト収集
    │   ├─ CLAUDE.md / AGENTS.md 読み取り
    │   ├─ 既存の docs/requirements/ 確認
    │   ├─ 引数にファイルパスがあれば読み込み
    │   └─ コードベース概要把握（既存PJの場合）
    │
Step 1: 入力パース＋充足度判定
    │   └─ 8セクション × 3段階（欠落/曖昧/十分）を判定
    │
Step 2-4: 適応的質問（最大3ラウンド）
    │   ├─ Round 1: WHO + WHY（アクター・目的）
    │   ├─ Round 2: WHAT（機能・データモデル・ルール）
    │   └─ Round 3: BOUNDARY（非機能・スコープ外・受け入れ条件）
    │
Step 5: ドラフト生成＋ユーザー確認
    │   └─ 全文を表示して修正・追加を受け付け
    │
Step 6: ファイル書き出し
    │   └─ docs/requirements/{slug}.md
    │
Step 7: 次のアクション案内
        └─ /issue-create docs/requirements/{slug}.md
```

## Step 0: コンテキスト収集

### プロジェクト状態の把握

- AGENTS.md の `## Project Context` があれば技術スタック・既存パターンを把握
- 引数にファイルパス（`.md`, `.txt` 等）が含まれる場合は内容を取得
- 既存 PJ の場合はコードベース構成を軽く調査（エンティティ名、ルート構成等）
- 既存の `docs/requirements/` を確認し、同領域の要件がないかチェック

### 既存要件との関係判定

| 状況 | 対応 |
|------|------|
| 既存要件なし | 新規作成 |
| 既存要件あり＋別領域 | 新規作成（既存を参照情報として利用） |
| 既存要件あり＋同領域 | ユーザーに確認: 上書き / 追補 / 中断 |

## Step 1: 入力パース＋充足度判定

ユーザーの入力（引数テキスト or 読み込んだファイル）を分析し、8セクションそれぞれの充足度を判定する。

### 8セクションと充足度基準

| # | セクション | 十分 | 曖昧 | 欠落 |
|---|-----------|------|------|------|
| 1 | 目的・背景 | Why が明確 | What はあるが Why がない | 言及なし |
| 2 | アクター | 名前＋役割が列挙 | 「ユーザー」のみ | 言及なし |
| 3 | ユースケース | 操作フロー記述あり | 機能名のみ | 言及なし |
| 4 | 機能要件 | 「〜できる」形式で3個以上 | 箇条書きだが曖昧 | 言及なし |
| 5 | データモデル | エンティティ＋関係 | エンティティ名のみ | 言及なし |
| 6 | 非機能要件 | 具体的な制約値あり | 「セキュリティに注意」等 | 言及なし |
| 7 | スコープ外 | 除外項目が明示 | — | 言及なし |
| 8 | 受け入れ条件 | テスト可能な条件あり | 「正しく動く」等 | 言及なし |

**判定結果に基づき、欠落セクションは必須質問、曖昧セクションは深掘り質問、十分セクションはスキップする。**

## Step 2: Round 1 — WHO + WHY

**目的・背景が欠落:**
> この機能はどんな課題を解決しますか？

**アクターが欠落:**
> この機能を使う人は誰ですか？ 複数の役割がある場合はすべて挙げてください。

**アクターが曖昧（「ユーザー」のみ）:**
> 「ユーザー」をもう少し具体化できますか？ 権限や操作範囲が異なる役割はありますか？

## Step 3: Round 2 — WHAT

Round 1 で確定したアクターを使って「誰が何をできるか」を聞く。

**ユースケースが欠落:**
> {アクターA} がこの機能を使うとき、どんな操作の流れになりますか？

**機能要件が欠落/曖昧:**
> 具体的にどんなことができる必要がありますか？（推測した候補を提示して過不足を確認）

**データモデルが欠落:**
> この機能で扱う主なデータは何ですか？ それぞれの関係性も教えてください。

**データモデルが曖昧:**
> {エンティティA} と {エンティティB} の関係はどうなりますか？

**ビジネスルールを検出:**
> 以下の理解で合っていますか？ 他にルールがあれば教えてください。

## Step 4: Round 3 — BOUNDARY

機能が確定してから境界を引く。

**非機能要件が欠落:**
> パフォーマンス、セキュリティ、既存システム連携で気にすべきことはありますか？

**スコープ外が欠落:**
> 今回の範囲に含めないものを明確にしたいです。「後回し」「別機能」はありますか？

**受け入れ条件が欠落/曖昧:**
> 機能要件から受け入れ条件のドラフトを自動生成し、ユーザーに過不足を確認する。
> 各機能に最低2条件（正常系1 + 異常系/境界値1）。テスト可能な形式で。

## Step 5: ドラフト生成＋ユーザー確認

### ラウンド省略

Step 1 で全セクション「十分」なら質問をスキップし直接ドラフト生成。

### 出力フォーマット

```markdown
# {機能名}

> このドキュメントは `/issue-create` の入力として使用できます。
> `/issue-create docs/requirements/{slug}.md`

## 1. 目的・背景
## 2. アクターとユースケース
## 3. 機能要件（各機能に受け入れ条件付き）
## 4. データモデル（エンティティ・関係・設計意図）
## 5. 非機能要件
## 6. スコープ外
## 7. 用語集
```

ドラフト全文を表示し、修正・追加を受け付ける。

## Step 6: ファイル書き出し

`docs/requirements/{slug}.md` に書き出し。`{slug}` は機能名からケバブケース英語で自動生成。

## Step 7: 次のアクション案内

```
要件定義を作成しました: docs/requirements/{slug}.md

次のステップ:
  /issue-create docs/requirements/{slug}.md この要件をもとに開発したい
```

## 質問戦略

### 適応的省略

| 入力の情報量 | 質問ラウンド数 |
|-------------|--------------|
| 走り書き（1-3行） | 3ラウンド |
| 中程度（段落レベル） | 1-2ラウンド |
| 詳細（構造化済み） | 0ラウンド（確認のみ） |

### 質問の形式

- 選択肢を提示できる場合は選択肢形式で負担軽減
- 自由回答には具体例を添える
- 既存コードから推測できる場合は推測結果を提示して確認

### 1ラウンドあたりの質問数

最大4問。優先度の高い欠落セクションから聞く。

## 出力品質チェックリスト

- [ ] 全機能要件に受け入れ条件が付いている（最低2条件/機能）
- [ ] 受け入れ条件に曖昧な表現がない
- [ ] データモデルの全エンティティに関係性と設計意図がある
- [ ] スコープ外セクションが空でない
- [ ] 用語集にドメイン固有の用語が含まれている
- [ ] 機能要件の粒度が Issue 分割可能（1機能 = 受け入れ条件 3-10個）

## 他のスキルとの連携

- `/requirement` → 要件定義書の生成（このスキル）
- `/issue-create` → 生成した要件定義書を入力として Issue 作成
- `/issue-flow #N` → Issue から自動開発
